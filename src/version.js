// Which update of the game this is: shown on the title screen, and compared with the site's
// version.json (which tools/build.mjs writes) to tell when this copy is out of date (see updates.js).
export const BUILD = 19;
